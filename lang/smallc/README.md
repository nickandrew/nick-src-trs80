# lang/smallc has been deleted

"lang/smallc" can be found in previous commits. Some of my own work
is in it (`c1a.c`) but this project is a dead end. There is no point
to run a C compiler on the TRS-80 architecture when sdcc exists for
linux.
