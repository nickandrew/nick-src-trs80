;readnews/asm: Assemble readnews, half C, half Z80
;
*GET	DOSCALLS
;
	ORG	5200H
START
	LD	HL,(HIMEM)
	LD	SP,HL
;
*GET	CINIT
*GET	CALL
*GET	DEBUG
;
*GET	ACTIVE
*GET	MAIN
*GET	NEWSRC
*GET	READ
*GET	VARS
;
*GET	SETPOS
*GET	READIDX
*GET	READTXT
*GET	ASKDISP
;
*GET	ATOI
*GET	STRCMP
*GET	CTYPE
*GET	LIBC
;
HIGHEST	DEFW	$+2
;
	END	START
