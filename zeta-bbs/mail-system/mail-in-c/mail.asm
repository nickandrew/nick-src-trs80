;mail: Assemble mail (read & send)
;
ZETA	EQU	0
;
*GET	DOSCALLS:0
;
	IF	ZETA
*GET	EXTERNAL
*GET	ASCII
;
	COM	'<Mail 1.0  04-Oct-87 Zeta>'
	ORG	PROG_START
	DEFW	BASE
	DEFW	THIS_PROG_END
	DEFW	0
	DEFW	0
;End of program load info.
	ORG	BASE
	ELSE
	ORG	5200H
;
	ENDIF
;
;;*GET	DEBUG:1
DEBUG	MACRO	#$STR
	ENDM
;
START
	LD	HL,(HIMEM)
	LD	SP,HL
;
*GET	CINIT:1
*GET	CALL:1
;
*GET	MAIL1
*GET	MAIL2
*GET	MAIL3
*GET	MAIL4
*GET	MAIL5
;
*GET	ASCTIME
*GET	ATOI:1
*GET	CTYPE:1
*GET	FREAD:1
*GET	FWRITE:1
*GET	FTELL:1
*GET	GETTIME
*GET	GETUID:1
*GET	GETUNAME
*GET	SEEKTO:1
*GET	STRCMP:1
*GET	STRLEN
;
	IF	ZETA
*GET	LIBCZ
	ELSE
*GET	LIBC:1
	ENDIF
;
_CMDLINE	DEFW	4318H
_NOREDIR	DEFW	0
;
HIGHEST	DEFW	$+2
;
	IF	ZETA
THIS_PROG_END	EQU	$
	ENDIF
;
	END	START
