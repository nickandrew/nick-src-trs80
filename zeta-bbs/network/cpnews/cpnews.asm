;cpnews/asm: Assemble cpnews, half C, half Z80
;
*GET	DOSCALLS
;
	ORG	5200H

*GET	DEBUG

START
	LD	HL,(HIMEM)
	LD	SP,HL
;
*GET	CINIT
*GET	CALL
;
*GET	CPNEWS1
*GET	CPNEWS2
;
*GET	SETPOS
;
*GET	ATOI
*GET	STRCMP
*GET	CTYPE
*GET	LIBC
;
HIGHEST	DEFW	$+2
;
	END	START
