;wc.asm: Assemble WC, compiled C
;
*GET	DOSCALLS
;
	ORG	5200H

*GET	DEBUG

START
	LD	HL,(HIMEM)
	LD	SP,HL
;
*GET	CINIT
*GET	CALL
;
*GET	WC1
;
*GET	ATOI
*GET	STRCMP
*GET	CTYPE
*GET	LIBC
;
HIGHEST	DEFW	$+2
;
	END	START
